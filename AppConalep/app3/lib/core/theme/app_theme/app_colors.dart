// core/theme/app_colors.dart
import 'package:flutter/material.dart';

class ThemeColors {
  // Base
  static const Color primary   = Color(0xFF4CAF50);
  static const Color secondary = Color(0xFF0288D1);

  // Claro
  static const Color background = Color(0xFFF5F5F5);
  static const Color surface    = Color(0xFFFFFFFF);
  static const Color textColor  = Color(0xFF212121);

  // Error
  static const Color baseErrorColor = Color(0xFFD32F2F);

  // Drawer — cambia según tema
  static const Color drawerLight = Color(0xFF4CAF50); // verde normal
  static const Color drawerDark  = Color(0xFF1B5E20); // verde oscuro
}